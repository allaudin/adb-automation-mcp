# packages

Installed-app queries on a connected device (`adb shell pm list packages`).
Install/uninstall and other package lifecycle operations aren't implemented yet.

::: adb_mcp.modules.packages.tools
