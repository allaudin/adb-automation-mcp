# permissions

Granting an Android runtime permission to an installed package (`adb shell
pm grant`). Revoking, checking, and listing permissions aren't implemented
yet.

::: adb_mcp.modules.permissions.tools
