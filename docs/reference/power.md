# power

The device's current high-level power state (`adb shell dumpsys power`),
deliberately minimal — wakefulness and, when reported, interactive state
only. Reboot/shutdown/sleep/wake control isn't implemented yet.

::: adb_automation_mcp.modules.power.tools
