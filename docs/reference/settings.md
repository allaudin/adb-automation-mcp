# settings

Reading Android `Settings` provider values (`adb shell settings get
NAMESPACE KEY`) — distinct from the system_properties module's
`getprop`/`setprop`. Writing (`put`/`delete`) isn't implemented yet.

::: adb_automation_mcp.modules.settings.tools
