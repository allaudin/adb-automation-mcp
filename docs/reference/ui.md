# ui

Retrieving the current Android UI hierarchy (`uiautomator dump`) as inline
XML — no temporary device-file location for the caller to know or manage.
Element finding and input actions aren't implemented here (see the input
module for touch injection).

::: adb_automation_mcp.modules.ui.tools
